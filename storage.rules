rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Desenvolvimento: qualquer usuário autenticado pode ler e escrever
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
